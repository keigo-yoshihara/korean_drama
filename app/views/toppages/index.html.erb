<div class="row">
  <aside class="col-sm-3">
    <% if logged_in? %>
      <%= link_to '投稿', new_drama_path, class: 'btn btn-lg btn-primary w-100' %>
    <% end %>
  </aside>
  <div class="col-sm-8">
    <%= render 'dramas/dramas', dramas: @dramas %>
  </div>
</div>
